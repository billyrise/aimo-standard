# Validator

Validator checks ensure artifacts conform to the standard.
The normative definition of checks is in `validator/rules/checks.md` and `checks.yaml`.
A minimal reference implementation is provided in `validator/src/validate.py`.
