# Pytest configuration for AIMO Standard
# Ensures both tests/ and validator/tests/ are collected on all environments (CI/local).
[pytest]
testpaths = tests validator/tests
python_files = test_*.py
python_functions = test_*
